{
  "name": "@nimbus/gateway",
  "version": "0.1.0",
  "private": true,
  "license": "AGPL-3.0-only",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target bun --compile --outfile ../../dist/nimbus-gateway",
    "typecheck": "tsc --noEmit",
    "lint": "biome check src/",
    "test": "bun test",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@nimbus-dev/sdk": "workspace:*",
    "@mastra/core": "^1.0.0",
    "@mastra/mcp": "^1.0.0",
    "@xenova/transformers": "^2.17.0",
    "pino": "^10.3.1",
    "sqlite-vec": "^0.1.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
